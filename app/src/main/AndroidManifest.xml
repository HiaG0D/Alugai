<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Alugaí">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity para Cadastrar um novo Local -->
        <activity
            android:name=".CadastroLocalActivity"
            android:exported="false" />

        <!-- Activity para Listar os Locais -->
        <activity
            android:name=".ListaLocaisActivity"
            android:exported="false" />

        <!-- Activity para Locar um Equipamento -->
        <activity
            android:name=".LocarEquipamentoActivity"
            android:exported="false" />

    </application>

</manifest>